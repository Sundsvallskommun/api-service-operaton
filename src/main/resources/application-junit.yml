#========================================
# Test profile settings (junit tests)
#========================================
logging:
  level:
    root: DEBUG
operaton.bpm:
  jmx-enabled: false
spring:
  jmx:
    enabled: false
